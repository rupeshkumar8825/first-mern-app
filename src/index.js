// in this we will render the react application 
import React from 'react';
import ReactDOM from 'react-dom';


// using the render method for this purpose 
ReactDOM.render(
  <>
    <h1>hi this is the index.js component of the react app  </h1>
  </>,
  document.getElementById("root")
);

